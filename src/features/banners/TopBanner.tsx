'use client';

// =============================================================================
// TopBanner — Top scrolling token marquee (left direction)
// =============================================================================

import TokenBanner from '@/components/common/TokenBanner';

export default function TopBanner() {
  return <TokenBanner direction="left" speed="normal" />;
}
