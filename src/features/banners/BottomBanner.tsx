'use client';

// =============================================================================
// BottomBanner — Bottom scrolling token marquee (right direction)
// =============================================================================

import TokenBanner from '@/components/common/TokenBanner';

export default function BottomBanner() {
  return <TokenBanner direction="right" speed="slow" />;
}
